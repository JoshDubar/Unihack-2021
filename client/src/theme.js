import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#fa448c",
    },
    secondary: {
      main: "#fd6",
    },
    text: {
      dark: "#333",
    },
    avatar: {
      main: "#333",
    },
  },
});

export default theme;
